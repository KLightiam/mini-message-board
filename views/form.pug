extends layout

block content
    form(method="post",action='/new')
        fieldset
            legend Message
            p
            label(for="userName") Username:
                input(type="text", name="userName", value="",placeholder="enter name")
            p
            label(for="userMessage") Message:
                input(type="text", name="userMessage", value="",placeholder="enter message")
            p.buttons
                input(type="submit", value="Submit Message")